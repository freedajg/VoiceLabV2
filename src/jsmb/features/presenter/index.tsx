// SEAM — the architect fills this in wave 3 (scenario launcher + transport).
import { EYEBROW } from "../../ui";

export function PresenterControls() {
  return (
    <div className="flex items-center gap-3 text-j-console-ink-2">
      <span className={EYEBROW}>Scenario controls</span>
      <span className="text-xs">Wired in wave 3</span>
    </div>
  );
}
